[defaults]
inventory = /opt/satellite-bfx-support/inventory
ask_pass = False
host_key_checking = False
remote_user = ec2-user
timeout = 30
stdout_callback = yaml

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False

